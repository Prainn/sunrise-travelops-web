import {
  businessCategoryTypes,
  resourceUnits,
  transportMethods,
} from "@/data/data";

/** 当前为本地原型实现，后续在此统一接入基础配置接口。 */
export const configurationService = {
  businessCategoryTypes,
  resourceUnits,
  transportMethods,
};
